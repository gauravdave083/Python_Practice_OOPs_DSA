def sum_of_two_numbers():

     a = float(input("Enter First Number: "))

     b = float(input("Enter second number: "))

     #sum of both the  numbers
     
     sum = a + b
      
     #int is used to change type so that print looks good  
     print("Sum of", int(a),"and", int(b), "is:", sum)

sum_of_two_numbers()